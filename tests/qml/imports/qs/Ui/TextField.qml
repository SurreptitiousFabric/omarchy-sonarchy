import QtQuick
import QtQuick.Controls as QQC

// Keep the native input API; only theme-specific colors are stubbed.
QQC.TextField {
  property color foreground: "white"
  property color accent: "blue"
}
